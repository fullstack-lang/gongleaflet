// generated by MultiCodeGeneratorNgService
import { Injectable } from '@angular/core';
import { HttpClientModule } from '@angular/common/http';
import { HttpClient, HttpHeaders } from '@angular/common/http';

/*
 * Behavior subject
 */
import { BehaviorSubject } from 'rxjs';
import { Observable, of } from 'rxjs';
import { catchError, map, tap } from 'rxjs/operators';

import { VisualCenterAPI } from './visualcenter-api';
import { VisualCenterDB } from './visualcenter-db';

@Injectable({
  providedIn: 'root'
})
export class VisualCenterService {

  httpOptions = {
    headers: new HttpHeaders({ 'Content-Type': 'application/json' })
  };

  // Kamar Raïmo: Adding a way to communicate between components that share information
  // so that they are notified of a change.
  VisualCenterServiceChanged: BehaviorSubject<string> = new BehaviorSubject("");

  private visualcentersUrl = 'http://localhost:8080/api/github.com/fullstack-lang/gongleaflet/go/v1/visualcenters';

  constructor(
    private http: HttpClient
  ) { }

  /** GET visualcenters from the server */
  getVisualCenters(): Observable<VisualCenterDB[]> {
    return this.http.get<VisualCenterDB[]>(this.visualcentersUrl)
      .pipe(
        tap(_ => this.log('fetched visualcenters')),
        catchError(this.handleError<VisualCenterDB[]>('getVisualCenters', []))
      );
  }

  /** GET visualcenter by id. Will 404 if id not found */
  getVisualCenter(id: number): Observable<VisualCenterDB> {
    const url = `${this.visualcentersUrl}/${id}`;
    return this.http.get<VisualCenterDB>(url).pipe(
      tap(_ => this.log(`fetched visualcenter id=${id}`)),
      catchError(this.handleError<VisualCenterDB>(`getVisualCenter id=${id}`))
    );
  }

  //////// Save methods //////////

  /** POST: add a new visualcenter to the server */
  postVisualCenter(visualcenterdb: VisualCenterDB): Observable<VisualCenterDB> {

		// insertion point for reset of pointers and reverse pointers (to avoid circular JSON)
    visualcenterdb.VisualLayer = {}
    visualcenterdb.VisualIcon = {}

		return this.http.post<VisualCenterDB>(this.visualcentersUrl, visualcenterdb, this.httpOptions).pipe(
			tap(_ => {
				// insertion point for restoration of reverse pointers
				this.log(`posted visualcenterdb id=${visualcenterdb.ID}`)
			}),
			catchError(this.handleError<VisualCenterDB>('postVisualCenter'))
		);
  }

  /** DELETE: delete the visualcenterdb from the server */
  deleteVisualCenter(visualcenterdb: VisualCenterDB | number): Observable<VisualCenterDB> {
    const id = typeof visualcenterdb === 'number' ? visualcenterdb : visualcenterdb.ID;
    const url = `${this.visualcentersUrl}/${id}`;

    return this.http.delete<VisualCenterDB>(url, this.httpOptions).pipe(
      tap(_ => this.log(`deleted visualcenterdb id=${id}`)),
      catchError(this.handleError<VisualCenterDB>('deleteVisualCenter'))
    );
  }

  /** PUT: update the visualcenterdb on the server */
  updateVisualCenter(visualcenterdb: VisualCenterDB): Observable<VisualCenterDB> {
    const id = typeof visualcenterdb === 'number' ? visualcenterdb : visualcenterdb.ID;
    const url = `${this.visualcentersUrl}/${id}`;

    // insertion point for reset of pointers and reverse pointers (to avoid circular JSON)
    visualcenterdb.VisualLayer = {}
    visualcenterdb.VisualIcon = {}

    return this.http.put(url, visualcenterdb, this.httpOptions).pipe(
      tap(_ => {
        // insertion point for restoration of reverse pointers
        this.log(`updated visualcenterdb id=${visualcenterdb.ID}`)
      }),
      catchError(this.handleError<VisualCenterDB>('updateVisualCenter'))
    );
  }

  /**
   * Handle Http operation that failed.
   * Let the app continue.
   * @param operation - name of the operation that failed
   * @param result - optional value to return as the observable result
   */
  private handleError<T>(operation = 'operation', result?: T) {
    return (error: any): Observable<T> => {

      // TODO: send the error to remote logging infrastructure
      console.error(error); // log to console instead

      // TODO: better job of transforming error for user consumption
      this.log(`${operation} failed: ${error.message}`);

      // Let the app keep running by returning an empty result.
      return of(result as T);
    };
  }

  private log(message: string) {

  }
}
