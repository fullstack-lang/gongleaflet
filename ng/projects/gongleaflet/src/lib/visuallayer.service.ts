// generated by ng_file_service_ts
import { Injectable, Component, Inject } from '@angular/core';
import { HttpClientModule } from '@angular/common/http';
import { HttpClient, HttpHeaders } from '@angular/common/http';
import { DOCUMENT, Location } from '@angular/common'

/*
 * Behavior subject
 */
import { BehaviorSubject } from 'rxjs';
import { Observable, of } from 'rxjs';
import { catchError, map, tap } from 'rxjs/operators';

import { VisualLayerDB } from './visuallayer-db';

@Injectable({
  providedIn: 'root'
})
export class VisualLayerService {

  httpOptions = {
    headers: new HttpHeaders({ 'Content-Type': 'application/json' })
  };

  // Kamar Raïmo: Adding a way to communicate between components that share information
  // so that they are notified of a change.
  VisualLayerServiceChanged: BehaviorSubject<string> = new BehaviorSubject("");

  private visuallayersUrl: string

  constructor(
    private http: HttpClient,
    private location: Location,
    @Inject(DOCUMENT) private document: Document
  ) {
    // path to the service share the same origin with the path to the document
    // get the origin in the URL to the document
	let origin = this.document.location.origin
    
	// if debugging with ng, replace 4200 with 8080
	origin = origin.replace("4200", "8080")

    // compute path to the service
    this.visuallayersUrl = origin + '/api/github.com/fullstack-lang/gongleaflet/go/v1/visuallayers';
   }

  /** GET visuallayers from the server */
  getVisualLayers(): Observable<VisualLayerDB[]> {
    return this.http.get<VisualLayerDB[]>(this.visuallayersUrl)
      .pipe(
        tap(_ => this.log('fetched visuallayers')),
        catchError(this.handleError<VisualLayerDB[]>('getVisualLayers', []))
      );
  }

  /** GET visuallayer by id. Will 404 if id not found */
  getVisualLayer(id: number): Observable<VisualLayerDB> {
    const url = `${this.visuallayersUrl}/${id}`;
    return this.http.get<VisualLayerDB>(url).pipe(
      tap(_ => this.log(`fetched visuallayer id=${id}`)),
      catchError(this.handleError<VisualLayerDB>(`getVisualLayer id=${id}`))
    );
  }

  //////// Save methods //////////

  /** POST: add a new visuallayer to the server */
  postVisualLayer(visuallayerdb: VisualLayerDB): Observable<VisualLayerDB> {

		// insertion point for reset of pointers and reverse pointers (to avoid circular JSON)

		return this.http.post<VisualLayerDB>(this.visuallayersUrl, visuallayerdb, this.httpOptions).pipe(
			tap(_ => {
				// insertion point for restoration of reverse pointers
				this.log(`posted visuallayerdb id=${visuallayerdb.ID}`)
			}),
			catchError(this.handleError<VisualLayerDB>('postVisualLayer'))
		);
  }

  /** DELETE: delete the visuallayerdb from the server */
  deleteVisualLayer(visuallayerdb: VisualLayerDB | number): Observable<VisualLayerDB> {
    const id = typeof visuallayerdb === 'number' ? visuallayerdb : visuallayerdb.ID;
    const url = `${this.visuallayersUrl}/${id}`;

    return this.http.delete<VisualLayerDB>(url, this.httpOptions).pipe(
      tap(_ => this.log(`deleted visuallayerdb id=${id}`)),
      catchError(this.handleError<VisualLayerDB>('deleteVisualLayer'))
    );
  }

  /** PUT: update the visuallayerdb on the server */
  updateVisualLayer(visuallayerdb: VisualLayerDB): Observable<VisualLayerDB> {
    const id = typeof visuallayerdb === 'number' ? visuallayerdb : visuallayerdb.ID;
    const url = `${this.visuallayersUrl}/${id}`;

    // insertion point for reset of pointers and reverse pointers (to avoid circular JSON)

    return this.http.put(url, visuallayerdb, this.httpOptions).pipe(
      tap(_ => {
        // insertion point for restoration of reverse pointers
        this.log(`updated visuallayerdb id=${visuallayerdb.ID}`)
      }),
      catchError(this.handleError<VisualLayerDB>('updateVisualLayer'))
    );
  }

  /**
   * Handle Http operation that failed.
   * Let the app continue.
   * @param operation - name of the operation that failed
   * @param result - optional value to return as the observable result
   */
  private handleError<T>(operation = 'operation', result?: T) {
    return (error: any): Observable<T> => {

      // TODO: send the error to remote logging infrastructure
      console.error(error); // log to console instead

      // TODO: better job of transforming error for user consumption
      this.log(`${operation} failed: ${error.message}`);

      // Let the app keep running by returning an empty result.
      return of(result as T);
    };
  }

  private log(message: string) {

  }
}
