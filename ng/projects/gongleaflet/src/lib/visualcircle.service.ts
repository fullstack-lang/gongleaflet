// generated by ng_file_service_ts
import { Injectable, Component, Inject } from '@angular/core';
import { HttpClientModule } from '@angular/common/http';
import { HttpClient, HttpHeaders } from '@angular/common/http';
import { DOCUMENT, Location } from '@angular/common'

/*
 * Behavior subject
 */
import { BehaviorSubject } from 'rxjs';
import { Observable, of } from 'rxjs';
import { catchError, map, tap } from 'rxjs/operators';

import { VisualCircleDB } from './visualcircle-db';

// insertion point for imports
import { VisualLayerDB } from './visuallayer-db'

@Injectable({
  providedIn: 'root'
})
export class VisualCircleService {

  httpOptions = {
    headers: new HttpHeaders({ 'Content-Type': 'application/json' })
  };

  // Kamar Raïmo: Adding a way to communicate between components that share information
  // so that they are notified of a change.
  VisualCircleServiceChanged: BehaviorSubject<string> = new BehaviorSubject("");

  private visualcirclesUrl: string

  constructor(
    private http: HttpClient,
    private location: Location,
    @Inject(DOCUMENT) private document: Document
  ) {
    // path to the service share the same origin with the path to the document
    // get the origin in the URL to the document
    let origin = this.document.location.origin

    // if debugging with ng, replace 4200 with 8080
    origin = origin.replace("4200", "8080")

    // compute path to the service
    this.visualcirclesUrl = origin + '/api/github.com/fullstack-lang/gongleaflet/go/v1/visualcircles';
  }

  /** GET visualcircles from the server */
  getVisualCircles(): Observable<VisualCircleDB[]> {
    return this.http.get<VisualCircleDB[]>(this.visualcirclesUrl)
      .pipe(
        tap(_ => this.log('fetched visualcircles')),
        catchError(this.handleError<VisualCircleDB[]>('getVisualCircles', []))
      );
  }

  /** GET visualcircle by id. Will 404 if id not found */
  getVisualCircle(id: number): Observable<VisualCircleDB> {
    const url = `${this.visualcirclesUrl}/${id}`;
    return this.http.get<VisualCircleDB>(url).pipe(
      tap(_ => this.log(`fetched visualcircle id=${id}`)),
      catchError(this.handleError<VisualCircleDB>(`getVisualCircle id=${id}`))
    );
  }

  //////// Save methods //////////

  /** POST: add a new visualcircle to the server */
  postVisualCircle(visualcircledb: VisualCircleDB): Observable<VisualCircleDB> {

    // insertion point for reset of pointers and reverse pointers (to avoid circular JSON)
    visualcircledb.VisualLayer = new VisualLayerDB

    return this.http.post<VisualCircleDB>(this.visualcirclesUrl, visualcircledb, this.httpOptions).pipe(
      tap(_ => {
        // insertion point for restoration of reverse pointers
        this.log(`posted visualcircledb id=${visualcircledb.ID}`)
      }),
      catchError(this.handleError<VisualCircleDB>('postVisualCircle'))
    );
  }

  /** DELETE: delete the visualcircledb from the server */
  deleteVisualCircle(visualcircledb: VisualCircleDB | number): Observable<VisualCircleDB> {
    const id = typeof visualcircledb === 'number' ? visualcircledb : visualcircledb.ID;
    const url = `${this.visualcirclesUrl}/${id}`;

    return this.http.delete<VisualCircleDB>(url, this.httpOptions).pipe(
      tap(_ => this.log(`deleted visualcircledb id=${id}`)),
      catchError(this.handleError<VisualCircleDB>('deleteVisualCircle'))
    );
  }

  /** PUT: update the visualcircledb on the server */
  updateVisualCircle(visualcircledb: VisualCircleDB): Observable<VisualCircleDB> {
    const id = typeof visualcircledb === 'number' ? visualcircledb : visualcircledb.ID;
    const url = `${this.visualcirclesUrl}/${id}`;

    // insertion point for reset of pointers and reverse pointers (to avoid circular JSON)
    visualcircledb.VisualLayer = new VisualLayerDB

    return this.http.put<VisualCircleDB>(url, visualcircledb, this.httpOptions).pipe(
      tap(_ => {
        // insertion point for restoration of reverse pointers
        this.log(`updated visualcircledb id=${visualcircledb.ID}`)
      }),
      catchError(this.handleError<VisualCircleDB>('updateVisualCircle'))
    );
  }

  /**
   * Handle Http operation that failed.
   * Let the app continue.
   * @param operation - name of the operation that failed
   * @param result - optional value to return as the observable result
   */
  private handleError<T>(operation = 'operation', result?: T) {
    return (error: any): Observable<T> => {

      // TODO: send the error to remote logging infrastructure
      console.error(error); // log to console instead

      // TODO: better job of transforming error for user consumption
      this.log(`${operation} failed: ${error.message}`);

      // Let the app keep running by returning an empty result.
      return of(result as T);
    };
  }

  private log(message: string) {

  }
}
